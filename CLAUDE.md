# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

This repository is currently empty scaffolding. It contains only `README.md` ("model cosmic") — no source code, build configuration, dependency manifests, or tests exist yet.

There are no build, lint, or test commands to document because no toolchain has been set up.

## Working in this repo

Since there is no established structure or convention yet, the first meaningful code or configuration added here will define the project's stack and layout. When that happens, update this file with:
- The language/framework chosen and how to install dependencies
- Build, lint, and test commands (including how to run a single test)
- The high-level architecture once multiple files/modules exist
